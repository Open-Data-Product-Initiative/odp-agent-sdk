schema: https://opendataproducts.org/odpr-v1.0/schema/odpr.yaml
version: "1.0"
kind: Recipe
recipe:
  metadata:
    id: RCP-PORTFOLIO-LOCALIZE-001
    name:
      en: Release Portfolio Localization
    description:
      en: Localize the example portfolio after provider readiness, explicit LLM permission, and review approval.
    tags:
      - portfolio
      - release
      - localization
  version: "1.0.0"
  type: release
  review:
    required: true
  execution:
    providerRef: claude
  steps:
    - id: localize
      command: portfolio.localize
      providerRef: claude
      model: claude-sonnet-4-5
      with:
        workspace: workspace/
        languages:
          - fi
          - sv
