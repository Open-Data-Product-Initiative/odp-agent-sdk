# Basic Portfolio Build

Example workspace for building a portfolio from source lane notes.

```bash
open-data-products recipe explain recipe.yaml --json
open-data-products recipe run recipe.yaml --dry-run --json
```

Expected outputs are written under `outputs-example/portfolio/` when execution
is approved and configured.
