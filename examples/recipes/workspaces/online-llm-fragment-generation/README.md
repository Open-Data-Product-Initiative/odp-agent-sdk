# Online LLM Fragment Generation

Example workspace for hosted-provider ODPC fragment generation.

```bash
open-data-products recipe run recipe.yaml --dry-run --json
open-data-products recipe run recipe.yaml --execute --allow-llm --approve-review --json
```

Execution requires provider configuration and the referenced API key
environment variable.
