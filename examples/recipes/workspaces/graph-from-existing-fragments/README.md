# Graph From Existing Fragments

Example workspace for generating an ODPG graph from ODPC fragments.

```bash
open-data-products recipe run recipe.yaml --dry-run --json
open-data-products recipe run recipe.yaml --execute --allow-llm --approve-review --json
```

This recipe uses guarded LLM-backed graph inference.
