# Local LLM Fragment Generation

Example workspace for local-provider ODPC fragment generation.

```bash
open-data-products recipe run recipe.yaml --dry-run --json
open-data-products recipe run recipe.yaml --execute --allow-llm --approve-review --json
```

Use this shape when the provider runs locally, such as Ollama.
